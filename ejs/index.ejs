<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/style.css">
    <title>Come 'n Shed</title>
    <script src="/scripts.js"></script>
</head>
<body>
<div style="border: solid black 2px" class="rounded">
    <div class="root rounded">
        <%-
            await include('header');
        %>
        <%-
            await include(`${type}/navigation`);
        %>
        <%
            if(type !== 'guest') {
        %>
            <%-
                await include(`${type}/identity`);
            %>
        <%
            }
        %>
        <%-
            await include(`${type}/content`);
        %>
    </div>
</div>
</body>
</html>